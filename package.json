{
  "name": "ecommerce-detail",
  "version": "1.0.0",
  "private": true,
  "description": "电商详情图生成系统",
  "scripts": {
    "dev": "chcp 65001 >nul & concurrently -n backend,frontend -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "chcp 65001 >nul & cd backend && npm run dev",
    "dev:frontend": "chcp 65001 >nul & cd frontend && npm run dev",
    "test:e2e": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "concurrently": "^9.1.2"
  }
}
